'use strict';

function findAdd (a, b) {
    const add = a + b;
    return add;
}

function findSub (a, b) {
    const sub = a + b;
    return sub;
}

function findMult (a, b) {
    const mult = a + b;
    return mult;
}


